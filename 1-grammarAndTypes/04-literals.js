//string literals
let truckLiteral = 'Ford';   //literally typed out 'Ford', making this a literal   aka: 'Hard-coding' a value

/*
let truck; 
truck = prompt('What truck are you?');  //truck here is not a string literal (but the prompt string is)
*/

//numeric literals
let myAge = 12;


//boolean literal
let isCandle = false;

//object literal
let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley'
};

console.log(soup.a);